# YMM4M v1.0.2 (release draft — unreleased)

**EXTERNAL_ONLY** setup-hardening release of YMM4M's own MIT software.
Ad-hoc signed, not notarized (Developer ID unavailable).

## Intended artifact

- `YMM4M-1.0.2-arm64.dmg` (+ `YMM4M-1.0.2-checksums.txt`, `YMM4M-1.0.2-manifest.json`)
- Bundle 1.0.2 (build 15), ARM64, `LSMinimumSystemVersion` 26.0
- License: `LEGAL-AUDIT-v1.0.1.md` still covers the DMG contents; the v1.0.2
  delta adds no new bundled content kinds (Brewfile is YMM4M-authored MIT),
  so a short addendum entry at release time suffices.

## Changes since v1.0.1 (see CHANGELOG.md 1.0.2 section)

- Brewfile ships inside YMM4M.app; setup remedy points at the exact copy.
- Preflight: `flex` covered, Homebrew-missing remedy first.
- Free-space gates (~10 GiB full / ~2 GiB prefix-only) before any download.
- Liveness heartbeat for long silent steps; host forwards `[gate]` too.
- Whitespace-safe compile-cache fallback with `YMM4M_COMPILE_CACHE_ROOT` override.

No runtime, patch, fixture, or catalog behavior changed. Pinned bootstrap
sources and hashes are identical to v1.0.1, so no staged runtime needs
re-validation.

## Verification (Apple M1 Max, macOS 26.5, CLT-only)

- `swift build` (incl. warnings-as-errors), contract tests (1 new),
  Python unit tests (4 new), metadata validators
- Bootstrap preflight aggregates correctly and exits 2 before downloads;
  bundled-layout Brewfile resolution verified with a synthetic bundle
- `--plan` output unchanged; `sh -n` on all touched scripts
- `tools/build-app.sh` output contains `RuntimeBootstrap/Brewfile`

Full log: `evidence/v1.0.2-setup-hardening-2026-09-11.md`.

## Still open (disclosed, not blockers for this EXTERNAL_ONLY build)

Same as v1.0.1, plus: full clean-machine bootstrap timing and the
multi-hour-build UX remain measured only by design (heartbeat + resume),
not by a fresh end-to-end run on this machine.
