# YMM4M v1.0.4 (release draft)

**EXTERNAL_ONLY** YMM4 v4.56 maintenance-train release of YMM4M's own MIT
software. Ad-hoc signed, not notarized (Developer ID unavailable).

## Artifact

- `YMM4M-1.0.4-arm64.dmg` (863,989 bytes, SHA-256
  `b9703d17dba4c36067943a7abed9564c23735b68eba1a0c011cc81e96b5a6b8a`)
  + `YMM4M-1.0.4-checksums.txt` + `YMM4M-1.0.4-manifest.json`
- Bundle 1.0.4 (build 17), ARM64, `LSMinimumSystemVersion` 26.0
- License: `LEGAL-AUDIT-v1.0.4.md` (addendum; incorporates v1.0.3 by reference)

## Changes since v1.0.3 (see CHANGELOG.md 1.0.4 section)

- New catalog family `4.56-runtime-boundary-1`: official 4.56.1.0
  Standard/Lite install as provisional candidates with confirmation;
  same-train micros with an unchanged boundary are covered automatically.
- Maintenance candidates accept their train anchor (`accepts()` `>=`).
- Install receipts stay readable on macOS 27 (atomic-only writes;
  unreadable receipts classify as unverifiable, fail-closed).

No runtime, patch, fixture, or host behavior changed. Pinned bootstrap
sources and hashes are identical to v1.0.3, so no staged runtime needs
re-validation.

## Verification (Apple M1 Max, macOS 27.0)

- Official 4.56.1.0 asset verification, candidate install and
  reclassification (both editions), CLI help (both editions), a visibly
  rendered Standard 4.56.1.0 main window, production-path Standard→Lite
  rollback (isolated store).
- `swift build` (incl. warnings-as-errors), contract tests in Japanese and
  English locales (train accept/deny, receipt re-read regression), Python
  unit tests (32), metadata validators, `--plan` / `--check-urls`, `sh -n`,
  `plutil -lint`, DMG read-only mount, strict ad-hoc signature, ARM64,
  catalog equality, forbidden-payload scan, GUI launch + clean quit.

Full log: `evidence/ymm4-4.56-maintenance-2026-09-21.md`.

## Still open (disclosed, not blockers for this EXTERNAL_ONLY build)

Same as v1.0.3, plus: all 4.56-specific behavior beyond install/CLI/
main-window (editing, playback, export, IME, voices) is unverified;
upstream v4.56.1.1 exists but was not installed.
