# YMM4M v1.0.5 (release draft)

**EXTERNAL_ONLY** simple-setup GUI audit release of YMM4M's own MIT
software. Ad-hoc signed, not notarized (Developer ID unavailable).

## Artifact

- `YMM4M-1.0.5-arm64.dmg` (SIZE bytes, SHA-256 `HASH`)
  + `YMM4M-1.0.5-checksums.txt` + `YMM4M-1.0.5-manifest.json`
- Bundle 1.0.5 (build 18), ARM64, `LSMinimumSystemVersion` 26.0
- License: `LEGAL-AUDIT-v1.0.5.md` (addendum; incorporates v1.0.4 by reference)

## Changes since v1.0.4 (see CHANGELOG.md 1.0.5 section)

- Rosetta functional check runs before the one-shot setup's file panels
  (shared live-check helper).
- Usage help announces YMM4's own first-run dialogs; setup-start status
  says not to quit; free-space note states the hour-scale time expectation.

No runtime, patch, fixture, catalog, or host behavior changed. Pinned
bootstrap sources and hashes are identical to v1.0.4, so no staged runtime
needs re-validation.

## Verification (Apple M1 Max, macOS 27.0)

- Fresh-state (settings cleared) host window in Japanese and English
  (v1.0.5 badge, 0/4 counter, gated launch buttons); new strings rendered
  without layout breakage.
- Live preflight remedy text (missing-tool exit 2 before downloads).
- `swift build` (incl. warnings-as-errors), contract tests in Japanese and
  English locales (shared live-check agreement), Python unit tests (32),
  metadata validators, DMG read-only mount, strict ad-hoc signature, ARM64,
  catalog equality, forbidden-payload scan, GUI launch + clean quit.

Full log: `evidence/simple-setup-gui-audit-2026-09-21.md`.

## Still open (disclosed, not blockers for this EXTERNAL_ONLY build)

Same as v1.0.4, plus concurrent-setup mutual exclusion (every step is
fail-closed; a double run wastes hours).
